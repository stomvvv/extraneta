[build]
builder = "DOCKERFILE"
dockerfilePath = "frontend/Dockerfile"
dockerContextPath = "frontend"

[deploy]
# No healthcheck for frontend — nginx serves static files, not a web API
restartPolicyType = "ON_FAILURE"
